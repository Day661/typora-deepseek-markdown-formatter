# Changelog

## 1.1.0 - 2026-09-01

- 增加独立的“DeepSeek 轻度清理”，快捷键为 `Ctrl+Alt+L`，右键菜单也可调用。
- 只处理极确定的错别字、孤立填充音和明显口误重复，并允许很小范围顺句；保留自然口语和个人感受，不改成书面稿。
- 增加本地改动幅度安全阈值；模型改动过多时停止覆盖原文。
- 原有 `Ctrl+Alt+M` 保守 Markdown 排版保持不变。

## 1.0.2 - 2026-09-01

- 将内置 Prompt 调整为“最小干预、原文零改写”的保守排版模式。
- 不再创造原文没有的标题，不再拆分句内枚举。
- 原样保留空白编号等占位符，禁止根据上下文自动补写。
- 增加 Prompt 约束回归测试。

## 1.0.1 - 2026-09-01

- 使用 Typora 原生 `File.editor.insertText` 接口写回选区。
- 修复 Typora 1.10.x 插入标题、列表等块级 Markdown 后触发 `detect data lost` 的错误。
- 保留 Typora 原生 `Ctrl+Z` 撤销行为。

## 1.0.0 - 2026-09-01

- 首个公开版本。
- 支持 DeepSeek 官方 API、固定 Markdown 排版 Prompt、快捷键和右键菜单。
- 自动覆盖选区，不提供 OCR、图片问答、通用聊天或 Prompt 自定义。
